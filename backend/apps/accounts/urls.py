"""
Accounts URL Configuration (Authentication)
"""
from django.urls import path

# Authentication endpoints will be implemented in Task 6
urlpatterns = [
    # path('login/', views.AuthLoginView.as_view(), name='auth_login'),
    # path('refresh/', views.AuthRefreshView.as_view(), name='auth_refresh'),
    # path('logout/', views.AuthLogoutView.as_view(), name='auth_logout'),
    # path('me/', views.AuthMeView.as_view(), name='auth_me'),
]
